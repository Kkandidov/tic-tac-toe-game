version: '3.9'
services:
  user_service_db_container:
    image: postgres:latest
    environment:
      POSTGRES_DB: 'users'
      POSTGRES_USER: 'admin'
      POSTGRES_PASSWORD: 'admin'
    ports:
      - '5432:5432'